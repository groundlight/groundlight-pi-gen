# Absolute basics
python3 python-is-python3

# don't install python3-pip or any libraries here, because pip and apt don't 
# get along, and most people prefer pip

# We'll set up a venv to get around this.
python3-venv

# Some nice-to-have utilities
iotop nethogs net-tools telnet
